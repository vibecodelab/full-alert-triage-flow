{
  "name": "orders-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small order pricing service used to exercise an alert-triage routine end to end.",
  "scripts": {
    "start": "node --env-file-if-exists=.env --import ./src/instrument.js src/server.js",
    "traffic": "node scripts/traffic.js",
    "test": "node --test",
    "smoke": "node --test smoke/service.smoke.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@sentry/node": "^10.74.0"
  }
}
